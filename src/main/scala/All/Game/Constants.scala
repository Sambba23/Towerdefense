package All.Game
import scalafx.Includes._
import scalafx.application.JFXApp3
import scalafx.scene.Scene
import scalafx.scene.control.{Button, Menu, MenuBar, MenuItem}
import scalafx.scene.paint.Color._
import scalafx.scene.shape._

object Constants {
  var windowSide = 700
  var balloonStartX = -70
  var balloonStartY = 250
  var ballRadius = 20
  var lineWidth = ballRadius*2 + 5
  var towerSide = 30
  var bullRadius = 2
  var basicRange = 200
  val placeGhostX = 563
  val placeGhostY = 5
  var downTime = 400
}
